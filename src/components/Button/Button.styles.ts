import {StyleSheet} from 'react-native';
import {colors} from '../../theme';

export const styles = StyleSheet.create({
  button: {
    padding: 20,
    borderRadius: 10,
    alignItems: 'center',
    width: '100%',
  },
  buttonTitle: {
    color: colors.white,
    fontSize: 16,
    fontWeight: 'bold',
  },
});
